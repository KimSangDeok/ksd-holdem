# 홀덤 연습장 프로젝트 지침

Claude가 이 프로젝트를 수정할 때 **매번 반드시 지켜야 할 규칙**. 사용자와 합의된 내용이므로 임의로 어기지 말 것.

## 항상 지킬 것

1. **모바일 동시 고려**: UI를 추가/수정할 때마다 모바일(≤620px)에서도 확인되게 반응형 CSS를 함께 수정한다. 미디어쿼리 단계: 1750px(가이드 1칸), 1350px(가이드 숨김+📚버튼), 950px(한 화면: 테이블+버튼 고정, 채팅은 하단 서랍 #chatHandle 탭으로 슬라이드), 620px(폰 최적화). 가로모드는 사용자가 거부함 — 만들지 말 것.
2. **브라우저/앱 자동 실행 금지**: 사용자가 직접 실행한다. Start-Process로 브라우저 열지 말 것.
3. **입력칸은 폰에서 16px 이상**: 아니면 iOS가 포커스 시 화면을 확대해버림.
4. **수정 후 괄호 균형 검증**: `<script>`/`<style>` 중괄호 짝 검사를 습관적으로 실행.
5. **용어를 왼쪽 패널에 추가하면 끝**: 채팅 밑줄 연동(termMap)은 패널의 `<b>` 태그에서 자동 생성되므로 별도 작업 불필요. 단, 항목 사이는 `<br>` 하나만.
6. **봇 성향을 바꾸면 코치 설명도 동기화**: `PRIME_TEXT`와 `situationText()`의 상대 성향 문구를 반드시 같이 수정.
7. **한국어 인코딩 규칙**: `.ps1`은 UTF-8 **BOM 포함** 저장, `.bat`은 **ASCII 전용**(한글 넣으면 깨짐).
8. **커밋 메시지는 항상 한글로**: 제목·본문 모두 한글로 쓴다. 합치기 요청(PR)의 제목과 설명도 한글로. 무엇을 왜 바꿨는지가 드러나게 쓸 것 — 예: `레이즈 버튼 크기 확대 (폰에서 눌리지 않던 문제)`.

## 구조

- **holdem.html**: 게임 전체가 든 단일 파일 (CSS/JS 인라인). 카드·족보·승률·봇 3명·즉시 판정·화면이 전부 이 안에 있고 **서버 없이 동작한다.**
- **기록 저장**: 설정·판수·핸드 기록·코칭 노트는 게임 밖에 저장된다. 저장 실패를 성공으로 표시하지 말 것.
- **코치**: 로컬에서 실행할 때만 동작한다. 비동기 티켓 방식(요청 → 티켓 → 결과 폴링)이며, **동기로 되돌리면 응답 대기 중 전체가 멈추므로 절대 되돌리지 말 것.**
- **코치 없이 열렸을 때**: "서버 꺼짐" 표시만 뜨고 게임·봇·즉시 판정·승률·음성은 그대로 동작하는 것이 정상이다. 이 동작을 깨뜨리지 말 것.

## 게임 설정 (사용자 합의값)

- 시작 스택 10,000 / 블라인드 100·200 / 100원 단위 베팅 / 파산 시 리필+자리 랜덤 재배치.
- 봇: 예송(프로급), 장송(초보·신중), 병송(초보·과감, 가끔 폭주). 모두 10시간+ 경험 초보 콘셉트 — 기본 심리(체크=약함에 찔러보기)는 읽을 줄 안다. 너무 극단적으로 만들지 말 것.
- 핸드 종료 시 전원 패 공개 + 폴드 이유 표시.
- 행동 확정 2단계(자동 상의): 이유 입력 → Enter/[🎓 코치평가]로만 코치 호출(입력 없이 확정하면 호출 0).
- 데이터 기록(체크박스, 설정에 저장): 켜져 있으면 핸드 기록+판수 카운트+10판마다 노트 갱신. 꺼진 판은 [💾 이번 판 기록]으로 소급 저장 가능.

## 사용자 스타일

- 초보 학습자. 용어는 물어보면 즉시 왼쪽 패널에 추가해준다.
- 코칭은 "맞으면 맞다고 인정, 억지 지적 금지" 원칙.
- 명칭: "프로필" 대신 **"코칭 노트"** 사용.
